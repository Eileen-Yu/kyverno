initContainer:
  extraArgs:
    - --loggingFormat=text

extraArgs:
  - --loggingFormat=text
  - --enablePolicyException=true
  - --exceptionNamespace=kyverno

cleanupController:
  rbac:
    clusterRole:
      extraResources:
        - apiGroups:
            - ""
          resources:
            - pods
